# agentic-wildfire-detection
Training-free wildfire detection on RGBT-3M. An agentic loop where Claude writes its own CV code, inspects the images it generates, and revises. 3× fire AP over SAM3 zero-shot, 33× over single-shot prompting. Four methods, one evaluator.
